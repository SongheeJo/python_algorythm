```python
X = input()                    # BaekjoonOnlineJudge
# word = [letter for letter in range(len(X)) if len(X)%10==0]
# print(word)
cnt = 0
for letter in X[0:len(X)]:     # 첫 글자부터 마지막 글자까지 한 글자씩 꺼내기
    cnt += 1
    print(letter, end="" if cnt%10!=0 else "\n") # 10글자가 아니면 같은 줄에 출력
    
```
